import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Magical Birthday Planner - Plan Unforgettable Kids' Parties",
  description: "AI-powered birthday party planner for children 0-12. Create magical celebrations with personalized themes, smart checklists, and stress-free planning.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>{children}</body>
    </html>
  )
}
